import { registerLocaleData } from '@angular/common'; import localeEsPe from '@angular/common/locales/es-PE'; import { ComponentFixture, TestBed } from '@angular/core/testing'; import { MAT_DIALOG_DATA, MatDialogRef } from '@angular/material/dialog'; import { CloseCashFormComponent } from './close-cash-form.component'; registerLocaleData(localeEsPe); describe('CloseCashFormComponent', () => { let fixture: ComponentFixture<CloseCashFormComponent>; beforeEach(async () => { await TestBed.configureTestingModule({ imports: [CloseCashFormComponent], providers: [{ provide: MAT_DIALOG_DATA, useValue: { expectedClosingAmount: '10.00' } }, { provide: MatDialogRef, useValue: { close: jasmine.createSpy('close') } }] }).compileComponents(); fixture = TestBed.createComponent(CloseCashFormComponent); fixture.detectChanges(); }); it('muestra el monto esperado', () => expect(fixture.componentInstance.difference()).toBe(0)); });
