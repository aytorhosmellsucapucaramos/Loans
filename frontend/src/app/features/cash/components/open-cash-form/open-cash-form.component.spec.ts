import { ComponentFixture, TestBed } from '@angular/core/testing'; import { MatDialogRef } from '@angular/material/dialog'; import { OpenCashFormComponent } from './open-cash-form.component'; describe('OpenCashFormComponent', () => { let fixture: ComponentFixture<OpenCashFormComponent>; beforeEach(async () => { await TestBed.configureTestingModule({ imports: [OpenCashFormComponent], providers: [{ provide: MatDialogRef, useValue: { close: jasmine.createSpy('close') } }] }).compileComponents(); fixture = TestBed.createComponent(OpenCashFormComponent); fixture.detectChanges(); }); it('crea el formulario', () => expect(fixture.componentInstance).toBeTruthy()); });
