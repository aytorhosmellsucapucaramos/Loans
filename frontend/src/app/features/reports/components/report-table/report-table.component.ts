import { CurrencyPipe, DatePipe, NgClass } from '@angular/common'; import { ChangeDetectionStrategy, Component, Input } from '@angular/core'; import { MatChipsModule } from '@angular/material/chips'; import { MatTableModule } from '@angular/material/table';
export type ReportColumn={key:string;label:string;kind?:'money'|'date'|'status'};
@Component({selector:'sp-report-table',imports:[CurrencyPipe,DatePipe,NgClass,MatChipsModule,MatTableModule],templateUrl:'./report-table.component.html',styleUrl:'./report-table.component.scss',changeDetection:ChangeDetectionStrategy.OnPush}) export class ReportTableComponent { @Input({required:true}) rows: Record<string, unknown>[]=[]; @Input({required:true}) columns: ReportColumn[]=[]; value(row:Record<string,unknown>,key:string): string|number|null { const value=row[key]; return typeof value==='string'||typeof value==='number'?value:value===null||value===undefined?null:String(value); } statusClass(value:string|number|null):string { return value===null?'':String(value); } label(value:string|number|null):string { if(value===null)return '—'; const labels:Record<string,string>={active:'Activo',paid:'Pagado',cancelled:'Cancelado',pending:'Pendiente',overdue:'Vencida',open:'Abierta',closed:'Cerrada',cash:'Efectivo',bank_transfer:'Transferencia',yape:'Yape',plin:'Plin',other:'Otro'};return labels[String(value)]??String(value); } }
