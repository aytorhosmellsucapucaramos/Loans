import { ChangeDetectionStrategy, Component, computed, inject, signal } from '@angular/core'; import { FormBuilder, ReactiveFormsModule, Validators } from '@angular/forms'; import { MatButtonModule } from '@angular/material/button'; import { MatIconModule } from '@angular/material/icon'; import { MatPaginatorModule, PageEvent } from '@angular/material/paginator'; import { MatProgressSpinnerModule } from '@angular/material/progress-spinner'; import { ActivatedRoute, Router } from '@angular/router'; import { finalize, type Observable } from 'rxjs'; import { ReportEmptyStateComponent } from '../../components/report-empty-state/report-empty-state.component'; import { ReportFiltersComponent } from '../../components/report-filters/report-filters.component'; import { type ReportColumn, ReportTableComponent } from '../../components/report-table/report-table.component'; import { ReportSummaryCardsComponent } from '../../components/report-summary-cards/report-summary-cards.component'; import type { CashReport, CollectionReport, InstallmentReport, LoanReport, ReportKind } from '../../models/report.model'; import { ReportsService } from '../../services/reports.service';
const kinds: ReportKind[]=['loans','installments','collections','cash']; const labels:Record<ReportKind,string>={loans:'Cartera de préstamos',installments:'Reporte de cuotas',collections:'Reporte de cobranza',cash:'Reporte de caja'};
@Component({selector:'sp-report-detail-page',imports:[ReactiveFormsModule,MatButtonModule,MatIconModule,MatPaginatorModule,MatProgressSpinnerModule,ReportEmptyStateComponent,ReportFiltersComponent,ReportSummaryCardsComponent,ReportTableComponent],templateUrl:'./report-detail-page.component.html',styleUrl:'./report-detail-page.component.scss',changeDetection:ChangeDetectionStrategy.OnPush}) export class ReportDetailPageComponent { private readonly route=inject(ActivatedRoute); private readonly router=inject(Router); private readonly api=inject(ReportsService); private readonly builder=inject(FormBuilder); readonly kind=signal<ReportKind>('loans'); readonly title=computed(()=>labels[this.kind()]); readonly filters=this.builder.nonNullable.group({fromDate:['',[Validators.pattern(/^$|^\d{4}-\d{2}-\d{2}$/)]],toDate:['',[Validators.pattern(/^$|^\d{4}-\d{2}-\d{2}$/)]],status:[''],customerId:['']},{validators:(group)=>{const from=group.get('fromDate')?.value as string;const to=group.get('toDate')?.value as string;return from&&to&&from>to?{dateRange:true}:null;}}); readonly loading=signal(true); readonly error=signal<string|null>(null); readonly report=signal<LoanReport|InstallmentReport|CollectionReport|CashReport|null>(null); readonly page=signal(1); readonly pageSize=signal(20); readonly total=computed(()=>this.report()?.page.pagination.total??0); readonly rows=computed<Record<string,unknown>[]>(()=>((this.report()?.page.items ?? []) as unknown as Record<string,unknown>[])); readonly columns=computed<ReportColumn[]>(()=>this.kind()==='loans'?[{key:'customerName',label:'Cliente'},{key:'disbursementDate',label:'Desembolso',kind:'date'},{key:'principalAmount',label:'Capital',kind:'money'},{key:'interestAmount',label:'Interés',kind:'money'},{key:'outstandingAmount',label:'Pendiente',kind:'money'},{key:'status',label:'Estado',kind:'status'}]:this.kind()==='installments'?[{key:'installmentNumber',label:'Cuota'},{key:'dueDate',label:'Vencimiento',kind:'date'},{key:'principalAmount',label:'Capital',kind:'money'},{key:'interestAmount',label:'Interés',kind:'money'},{key:'outstandingAmount',label:'Pendiente',kind:'money'},{key:'status',label:'Estado',kind:'status'}]:this.kind()==='collections'?[{key:'paymentDate',label:'Fecha',kind:'date'},{key:'paymentMethod',label:'Método',kind:'status'},{key:'amount',label:'Monto',kind:'money'},{key:'operationReference',label:'Referencia'}]:[{key:'openedAt',label:'Apertura',kind:'date'},{key:'status',label:'Estado',kind:'status'},{key:'incomeAmount',label:'Ingresos',kind:'money'},{key:'expenseAmount',label:'Egresos',kind:'money'},{key:'reversalAmount',label:'Reversiones',kind:'money'},{key:'expectedBalance',label:'Esperado',kind:'money'},{key:'differenceAmount',label:'Diferencia',kind:'money'}]); constructor(){const value=this.route.snapshot.paramMap.get('type');if(!value||!kinds.includes(value as ReportKind)){void this.router.navigate(['/reports']);return;}this.kind.set(value as ReportKind);this.load();} cards():{label:string;value:string|number;money?:boolean}[]{const report=this.report();if(!report)return[];if(this.kind()==='loans'){const data=report as LoanReport;return[{label:'Préstamos',value:data.totals.loanCount},{label:'Capital',value:data.totals.principalAmount,money:true},{label:'Intereses',value:data.totals.interestAmount,money:true},{label:'Pendiente',value:data.totals.outstandingAmount,money:true}];}if(this.kind()==='installments'){const data=report as InstallmentReport;return[{label:'Pendientes',value:data.totals.pendingCount},{label:'Pagadas',value:data.totals.paidCount},{label:'Vencidas',value:data.totals.overdueCount},{label:'Total pendiente',value:data.totals.outstandingAmount,money:true}];}if(this.kind()==='collections'){const data=report as CollectionReport;return[{label:'Pagos',value:data.totals.paymentCount},{label:'Total cobrado',value:data.totals.totalAmount,money:true},{label:'Métodos',value:data.totals.byMethod.length},{label:'Días con cobros',value:data.totals.byDay.length}];}const data=report as CashReport;return[{label:'Ingresos',value:data.totals.incomeAmount,money:true},{label:'Egresos',value:data.totals.expenseAmount,money:true},{label:'Reversiones',value:data.totals.reversalAmount,money:true},{label:'Saldo esperado',value:data.totals.expectedBalance,money:true}];} load():void{if(this.filters.invalid){this.filters.markAllAsTouched();return;}this.loading.set(true);this.error.set(null);const raw=this.filters.getRawValue();const base={page:this.page(),pageSize:this.pageSize(),fromDate:raw.fromDate||undefined,toDate:raw.toDate||undefined};const request:Observable<LoanReport|InstallmentReport|CollectionReport|CashReport>=this.kind()==='loans'?this.api.loans({...base,customerId:raw.customerId.trim()||undefined,status:(raw.status||undefined) as never}):this.kind()==='installments'?this.api.installments({...base,status:(raw.status||undefined) as never}):this.kind()==='collections'?this.api.collections(base):this.api.cash({...base,status:(raw.status||undefined) as never});request.pipe(finalize(()=>this.loading.set(false))).subscribe({next:(report)=>this.report.set(report),error:()=>this.error.set('No fue posible cargar el reporte solicitado.')});} apply():void{this.page.set(1);this.load();} clear():void{this.filters.reset({fromDate:'',toDate:'',status:'',customerId:''});this.page.set(1);this.load();} changePage(event:PageEvent):void{this.page.set(event.pageIndex+1);this.pageSize.set(event.pageSize);this.load();} back():void{void this.router.navigate(['/reports']);} }
