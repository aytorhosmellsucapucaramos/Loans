import { TestBed } from '@angular/core/testing';
import { HttpClientTestingModule, HttpTestingController } from '@angular/common/http/testing';
import { ReportsService } from './reports.service';
describe('ReportsService', () => { let service: ReportsService; let http: HttpTestingController; beforeEach(() => { TestBed.configureTestingModule({ imports:[HttpClientTestingModule] }); service=TestBed.inject(ReportsService); http=TestBed.inject(HttpTestingController); }); afterEach(()=>http.verify()); it('consulta el resumen real',()=>{service.summary().subscribe((result)=>expect(result.totalCollected).toBe('10.00')); const request=http.expectOne('/api/reports/summary'); request.flush({success:true,data:{activeCustomers:1,activeLoans:1,totalDisbursed:'10.00',totalOutstanding:'5.00',totalCollected:'10.00',overdueInstallments:0,paymentsToday:{count:1,totalAmount:'10.00'},currentCashBalance:'0.00'},errors:[]});}); it('incluye filtros y paginación en cartera',()=>{service.loans({page:2,pageSize:10,fromDate:'2026-01-01',toDate:'2026-01-31',status:'active'}).subscribe(); const request=http.expectOne((entry)=>entry.url==='/api/reports/loans'); expect(request.request.params.get('page')).toBe('2'); expect(request.request.params.get('status')).toBe('active'); expect(request.request.params.get('fromDate')).toBe('2026-01-01'); request.flush({success:true,data:{totals:{loanCount:0,principalAmount:'0.00',interestAmount:'0.00',outstandingAmount:'0.00',byStatus:{active:0,paid:0,cancelled:0}},page:{items:[],pagination:{page:2,pageSize:10,total:0,totalPages:0}}},errors:[]});}); });
